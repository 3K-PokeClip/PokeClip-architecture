# Pokeclip M1 수직 슬라이스 실행 계획

**이 문서의 목적**: 기능명세서 M1(A1·A2(2트랙)·A5 / B1·B2·B3·B6 / D1 / I1 — "OBS에서 쏜 방송이 우리 웹에서 멀티오디오로 재생되고 되감긴다")을 **무엇부터, 어떤 순서로** 실행할지 확정한다. 순서의 논리는 하나 — 계약 → 언블록 스텁 → 최대 리스크 우선 검증 → 병렬 → 가는 관통을 4번 반복. "다 만들고 마지막에 붙이기"의 정반대다.

**같이 볼 문서**: [9_기능명세서](9_Pokeclip_기능명세서.md) 3장(마일스톤) · [11_역할분담](11_Pokeclip_역할분담.md) · [ADR-003](adr/ADR-003_미디어서버_Go.md)

---

## 0단계 — 계약 먼저 (첫 2~3일, 코드보다 먼저)

M1에 필요한 계약 4개만 확정한다. 전부 1번이 초안, 소비자인 2·3번이 해당 경계를 리뷰한다.

| 계약 | 내용 |
|---|---|
| SRT 입력 규약 | **형식**: `streamid`=`#!::r=<26자 Crockford Base32>,m=publish` · `passphrase`=32자 랜덤/AES-256 · 트랙 매핑=**6트랙**([ADR-017](adr/ADR-017_오디오트랙6.md)) · **저장**: streamid=해시(PG)/passphrase=Secrets Manager([ADR-018](adr/ADR-018_스트림키저장.md)) · **전달**: 페어링 코드 · **방송 중 재발급 불가**([ADR-019](adr/ADR-019_스트림키형식전달.md)) · **인코딩 규약(2026-07-25 추가, [ADR-020](adr/ADR-020_LLHLS_DVR파라미터.md))**: 컨테이너 MPEG-TS · 비디오 H.264 · 오디오 **전 트랙 AAC** · **인코더 keyint = 2초 고정**(30fps `-g 60` / 60fps `-g 120`) — **플러그인이 강제 지정**, 스트리머 설정에 맡기지 않음. *(2026-07-25 자료조사로 1초→2초 정정: Apple 오소링 스펙 권고=2초, 1초는 키프레임 2배로 화질·비트레이트 손실. 2초도 4초 세그먼트의 약수라 정렬 유지)* *근거: HLS 세그먼트는 키프레임에서만 절단 가능 → 세그먼트 4s는 GOP의 정수배여야 하며, GOP가 임의값(예 3s)이면 세그먼트가 드리프트해 DVR 900개·hot 창·지연 예산이 전부 어긋난다.* — *페어링 코드 구체값 확정 완료([ADR-019](adr/ADR-019_스트림키형식전달.md) §3, 2026-07-24): 8자 `XXXX-XXXX`·10분·일회용·발급 3회/분(계정)·교환 5회/분(IP)* |
| B7 세그먼트 계약 | 디렉토리·매니페스트 규격 — 폴백 헤지의 전제. **세그먼트 인덱스**(매니페스트 생성의 단일 진실원, S3 LIST 금지)의 필드·상태전이·소유는 **[계약-세그먼트인덱스](contracts/계약-세그먼트인덱스.md)** (1 ↔ 3) |
| LL-HLS/DVR 재생 규약 | 매니페스트 형태·윈도우 길이·세그먼트 URL·시킹 기준 (역할분담 계약 3번) → **초안 완성: [계약3-LLHLS-DVR재생규약](contracts/계약3-LLHLS-DVR재생규약.md)** (세그먼트 4s·part 0.5s·DVR 1h·catch-up 끄기, [ADR-020](adr/ADR-020_LLHLS_DVR파라미터.md)) |
| 방송 생명주기 이벤트 | 계약 9 envelope·SNS FIFO→Chat/Clip 전용 SQS FIFO·DLQ·멱등성·역순 방어 |

## 1단계 — 언블록 장치 두 개 (첫 주)

| 순서 | 작업 | 담당 | 왜 먼저 |
|---|---|---|---|
| 1-1 | I1 docker-compose 스켈레톤 (PG·Redis·Media 자리) | 1번 (+3번) | 전원의 개발 환경 — 모든 것의 바닥 |
| 1-2 | 정적 세그먼트 스텁 서버 — 미리 만든 CMAF 세그먼트+매니페스트를 계약 규격대로 서빙만 | 1번 (반나절) | 2번이 진짜 Media Origin을 기다리지 않고 플레이어 개발 시작 |

## 2단계 — 3트랙 병렬

**핵심 트릭**: 플러그인 없이 Media Origin부터 검증한다. OBS 내장 커스텀 출력이 SRT를 지원하므로(단일 오디오), 표준 OBS로 쏘면서 B1~B3를 먼저 개발한다 — 최대 리스크(자체 미디어 계층)의 판정이 플러그인 완성을 기다릴 이유가 없다. *(2026-07-27 갱신: MediaMTX 실증으로 수신·먹싱·DVR은 검증 완료 — 남은 리스크는 **자체 합성 레이어**(콜드 프리픽스+핫 패스스루)와 티어드 서빙이다.)*

| 트랙 | 순서 | 담당 |
|---|---|---|
| 미디어 서버 | **MediaMTX 배포(SRT 수신·CMAF·LL-HLS·DVR — EC2 실증 완료)** → 사이드카(세그먼트 감시 → 인덱스 기록 → S3 즉시 업로드) → **매니페스트 합성 레이어**(콜드=인덱스 + 핫=MediaMTX 패스스루) → 정규 URL + origin failover 서빙 | 1번 |
| 플레이어 | hls.js LL-HLS 스파이크(스텁 상대로) → DVR 시킹 UI → 멀티오디오 트랙 전환 UI | 2번 |
| 데이터·선행 | compose에 PG·Redis 편입 → 스키마 v0 마이그레이션(broadcasts부터) → B6 Clip 전용 FIFO 소비 스텁(계약 9 수신·멱등 로그) → 여유 시 M2 선행(H1 구글 로그인 뼈대) | 3번 |

플러그인은 미디어 서버가 B3까지 선 다음 착수: A1 동시 송출(TS 먹싱) → A2 멀티오디오 2트랙 → A5 자동 재연결. **베이스는 `sorayuki/obs-multi-rtmp` 포크**(인코더 공유·SRT 출력·자동 재연결·Qt Dock 재사용), 코드는 별도 private repo `pokeclip-obs-plugin`(GPL-2.0).

## 3단계 — 통합 게이트 4개 (순서 고정)

| 게이트 | 검증 서사 | 의미 |
|---|---|---|
| **G1** | 표준 OBS SRT → Media Origin → 웹에서 라이브 재생 (단일 오디오) | 최초 엔드투엔드 관통 |
| **G2** | DVR 1시간 시킹 동작 | **[07-25 선행 실증]** MediaMTX config로 되감기 확인 완료(catch-up 끈 플레이어) → 이 게이트는 이제 **자체 합성·티어드 서빙 판정**(정규 URL + EC2 404→S3 failover)으로 의미 이동 |
| **G3** | 자체 플러그인 송출 교체 + 2트랙 → 플레이어 트랙 전환 | 멀티오디오 차별점 증명 |
| **G4** | 송출 중 회선 단절 → A5 재연결 복구 + B6 논리 이벤트 1개 생성(불명확 시 동일 `eventId` 재시도) → Chat/Clip 전용 FIFO가 같은 시작/종료 이벤트를 각각 수신·멱등 처리 | M1 서사 완성 |

## 폴백 규칙 (사전 합의)

G2에 **시간 상한**을 둔다(구체 수치는 멘토와 확정). **[07-27 갱신] DVR 되감기 자체는 MediaMTX로 실증 완료**되어 원래의 "DVR 구조적 실패" 리스크는 사라졌고, 상한은 **자체 합성 레이어(매니페스트·티어드 서빙)**에 적용한다. 초과 시 B7 세그먼트 계약대로 **합성 레이어를 단순화**(MediaMTX 로컬 창을 1h로 키워 티어링 없이 서빙)하고 M2 이후 일정을 보존한다. 이 전환 결정은 사전 합의 사항이며, 시점 판단은 게이트 결과로만 한다.

## 운영 노트

- M1 기간엔 매일 짧은 싱크 (역할분담 공통 규칙)
- 게이트 통과는 데모 영상 또는 재현 절차로 기록해 심사 증빙으로 재활용한다
